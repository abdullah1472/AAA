import 'package:flutter/material.dart';

class RequestsListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('List of Requests'),
      ),
      body: Center(
        child: Text('Here all submitted requests will be displayed.'),
      ),
    );
  }
}
