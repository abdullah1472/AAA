import 'package:flutter/material.dart';

class RequestFormPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Submit a Request'),
      ),
      body: Center(
        child: Text('Form to submit a new maintenance request.'),
      ),
    );
  }
}
